import 'package:flutter/material.dart';

class Recordaudio extends StatelessWidget {
  const Recordaudio({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Record Audio Page'),
    );
  }
}